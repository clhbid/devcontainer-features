{
  "id": "1password-commit-signing",
  "version": "1.1.0",
  "name": "1Password commit signing",
  "description": "Signs git commits with an SSH key held in 1Password on a macOS host. The consumer bind-mounts the 1Password agent socket at /ssh-agent.sock and points SSH_AUTH_SOCK at it; the Feature does everything inside the container.",
  "documentationURL": "https://github.com/clhbid/devcontainer-features/tree/main/src/1password-commit-signing",
  "options": {},
  "postStartCommand": "/usr/local/share/1password-commit-signing/post-start.sh"
}
